import { StoreParams } from "../../../models/StoreParams"
import { Data } from "../../../data/data"

export const selection = (storeParams: StoreParams, storeDispatch: any, data: Data) => {
    console.log(storeParams.fullClick)
}